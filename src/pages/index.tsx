import React from 'react';
import SubTitle from "@/components/SubTitle";

const Home = () => {
    return (
        <div>
            <SubTitle subtitle={"Test"} width="30px"/>
        </div>
    );
};

export default Home;