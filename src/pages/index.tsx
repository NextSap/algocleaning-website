import React from 'react';
import Header from "@/components/Header";

const Home = () => {
    return (
        <div>
            <Header contactId="CONTACTID"/>
        </div>
    );
};

export default Home;